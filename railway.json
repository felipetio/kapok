{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd frontend && npm ci && npm run build && cd ../backend && uv sync && uv run python manage.py collectstatic --noinput && uv run python manage.py migrate --noinput"
  },
  "deploy": {
    "runtime": "V2",
    "startCommand": "cd backend && uv run uvicorn config.asgi:application --host 0.0.0.0 --port $PORT",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
